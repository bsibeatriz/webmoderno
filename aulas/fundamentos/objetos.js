const prod1 = {}; // {} = objeto (escrito de forma literal) coleção de chave,valor

prod1.nome = "Celular Ultrafino"; // criei a chave .nome de forma dinamica
prod1.valor = 4998.9;
prod1["Desconto Legal"] = 0.4; //evitar atributos com espaço

console.log(prod1);

const prod2 = {
  nome: "Camisa polo",
  valor: 79.9,
};

console.log(prod2);
